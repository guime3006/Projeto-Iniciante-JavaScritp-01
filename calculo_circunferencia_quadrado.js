// Area da Circunferencia

/*

Aqui é feito o calculo da area da circunferencia do quadrado

*/

const PI = 3.14
let raio = 30
let area = PI * (raio * raio)
console.log(`Area: ${area} m2`)

raio = 10
area = Math.PI * (raio * raio)
console.log(`Area: ${area.toFixed(2)} m2`)